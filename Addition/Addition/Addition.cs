using System;
public class Addition
{


    public static void Main()
    { // varible (byte,short,int,long)
        // other var for numbers with decimal points and having double the same number(float(f), double, decimal(m))
        // varibles above go from smallest amount of digits to biggest
        // var for characters,text and t/f (char,string,bool)
        int num1;
        int num2 = 200;
        Console.Write("how much");
        num1 = int.Parse(Console.ReadLine());


        int result = num1 + num2;
        Console.WriteLine("i need bout:" + result);
        

        





    }
}